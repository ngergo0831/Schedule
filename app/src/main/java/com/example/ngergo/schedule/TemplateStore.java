package com.example.ngergo.schedule;

public class TemplateStore {
              /*tl= rootView.findViewById(R.id.table_id);
                tl.setPadding(15,15,15,15);

                FrameLayout.LayoutParams lp = new FrameLayout.LayoutParams(
                        TableRow.LayoutParams.WRAP_CONTENT,
                        TableRow.LayoutParams.WRAP_CONTENT
                );
                CardView cardView = new CardView(getContext());
                cardView.setLayoutParams(lp);
                cardView.setRadius(9);
                cardView.setContentPadding(15, 15, 15, 15);
                cardView.setMaxCardElevation(15);
                cardView.setElevation(9);

                tl.setLayoutParams(lp);
                tl.setStretchAllColumns(true);

                int index = 0;

                while(index < m2.jaratok.size())
                {
                    TableRow tr = new TableRow(getActivity());

                    for(int k=0; k<1;k++)
                    {
                        if(index<m2.jaratok.size())
                        {
                            final Button btn = new Button(getContext());
                            btn.setText(m2.jaratok.get(index).getName());
                            TableRow.LayoutParams params = new TableRow.LayoutParams(TableRow.LayoutParams.WRAP_CONTENT,TableRow.LayoutParams.WRAP_CONTENT);
                            params.setMargins(15,15,15,15);
                            btn.setTextColor(getResources().getColor(colorAccent));
                            btn.setBackgroundColor(getResources().getColor(colorPrimary));
                            btn.setLayoutParams(params);
                            tr.addView(btn);

                            Button btn2 = new Button(getContext());
                            btn2.setText(m2.jaratok.get(index).getRouteNumber());
                            btn2.setBackgroundColor(Color.TRANSPARENT);
                            btn2.setLayoutParams(params);
                            btn2.setTextAlignment(View.TEXT_ALIGNMENT_VIEW_START);
                            tr.addView(cardView);
                            index++;
                        }
                    }
                    tl.addView(tr);
                }*/
}
